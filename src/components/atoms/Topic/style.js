import styled from 'styled-components';
import { SIZES, PRIMARY_COLOR } from '../../../styles/theme';

export const StyledTopic = styled.p`
  font-size: ${SIZES.md};
`;

export const StyledTopicItem = styled.span`
  color: ${PRIMARY_COLOR};
  font-weight: 700;
`;
